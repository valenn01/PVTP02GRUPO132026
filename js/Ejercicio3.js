const radioEsfera = 4; 

const volumenEsfera = (4 / 3) * Math.PI * Math.pow(radioEsfera, 3);

console.log(`El volumen de una esfera con radio de ${radioEsfera} m es: ${volumenEsfera.toFixed(2)} m³.`);

console.log("\n--------------------------------------------------\n");
